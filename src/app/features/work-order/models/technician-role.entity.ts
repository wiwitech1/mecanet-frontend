/**
 * Roles posibles de un técnico en una orden de trabajo
 */
export enum TechnicianRole {
    MEMBER = 'MEMBER'
}

/**
 * Estados posibles de un técnico en una orden de trabajo
 */
export enum TechnicianStatus {
    JOINED = 'JOINED',
    WITHDRAWN = 'WITHDRAWN'
} 