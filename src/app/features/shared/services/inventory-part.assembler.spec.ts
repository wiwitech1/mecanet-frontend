import { InventoryPartAssembler } from './inventory-part.assembler';

describe('InventoryPartAssembler', () => {
  it('should create an instance', () => {
    expect(new InventoryPartAssembler()).toBeTruthy();
  });
});
