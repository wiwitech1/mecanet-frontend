import { PurchaseOrderAssembler } from './purchase-orders.assembler';

describe('PurchaseOrderAssembler', () => {
  it('should create an instance', () => {
    expect(new PurchaseOrderAssembler()).toBeTruthy();
  });
});
