import { InventoryPartEntity } from './inventory-part.entity';

describe('InventoryPartEntity', () => {
  it('should create an instance', () => {
    expect(new InventoryPartEntity({})).toBeTruthy();
  });
});
